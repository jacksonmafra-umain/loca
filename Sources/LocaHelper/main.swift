import Foundation
import LocaCore

// Filled in by milestone 1. Until then the target exists so the package
// resolves and the bundle layout can be assembled and signed.
FileHandle.standardError.write(
    Data("LocaHelper: core \(LocaCoreVersion.current), not yet implemented\n".utf8)
)
exit(0)
