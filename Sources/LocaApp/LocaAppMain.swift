import Foundation
import LocaCore

// Filled in by milestone 1 (bundle skeleton) and milestone 3 (the real window).
FileHandle.standardError.write(
    Data("LocaApp: core \(LocaCoreVersion.current), not yet implemented\n".utf8)
)
exit(0)
